# PostgreSQL Configuration
POSTGRES_USER=ghost
POSTGRES_PASSWORD=bullet2026
POSTGRES_DB=ghost_bullet

# UI Configuration
SECRET_KEY=supersecret_ghost_bullet_key

# Database URL used by the services (usually constructed automatically in docker-compose)
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db/${POSTGRES_DB}
